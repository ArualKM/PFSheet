=== EDGE CASE SCENARIO ===

Initial state (base):
- spheres: [ { id: "sphere-fire", name: "Fire", system: "Magic" }, { id: "sphere-air", name: "Air", system: "Magic" } ]
- drawbacks: [ "Somatic" ]
- drawbackMeta: { "Somatic": { system: "Magic", appliesTo: { kind: "sphere", id: "sphere-fire" } } }

Side A (mine) edits:
- drawbackMeta["Somatic"].appliesTo = { kind: "sphere", id: "sphere-air" }
  (Retargets the drawback to the Air sphere instead)

Side B (theirs) edits:
- spheres = [ { id: "sphere-fire", name: "Fire", system: "Magic" } ]
  (Deletes the Air sphere)

=== THREE-WAY MERGE EXECUTION ===

mergeObject() is called for the spheres block:

1. Iterate over all keys in base ∪ mine ∪ theirs
   Keys: casterClasses, spheres, talents, drawbacks, boons, drawbackMeta, boonMeta, etc.

2. For each key, call mergeValue(base[k], mine[k], theirs[k], path, conflicts)

3. Key: "spheres" (an entity array)
   - base.spheres = [fire, air]
   - mine.spheres = [fire, air] (unchanged by side A)
   - theirs.spheres = [fire] (side B deleted air)
   - mergeArray() → both sides have air or don't; air is deleted cleanly → merged.spheres = [fire]

4. Key: "drawbackMeta" (a plain object / record)
   - base.drawbackMeta = { "Somatic": { system: "Magic", appliesTo: { kind: "sphere", id: "sphere-fire" } } }
   - mine.drawbackMeta = { "Somatic": { system: "Magic", appliesTo: { kind: "sphere", id: "sphere-air" } } }
   - theirs.drawbackMeta = { "Somatic": { system: "Magic", appliesTo: { kind: "sphere", id: "sphere-fire" } } } (unchanged)
   - mergeObject() called recursively on drawbackMeta
     - Iterate over key "Somatic"
       - mergeValue(base["Somatic"], mine["Somatic"], theirs["Somatic"], ..., ...)
       - base["Somatic"] ≠ mine["Somatic"] (appliesTo changed)
       - base["Somatic"] = theirs["Somatic"] (no change on their side)
       - Condition: deepEqual(mine, base)? NO
       - Condition: deepEqual(theirs, base)? YES → return theirs["Somatic"]
       - Result: { system: "Magic", appliesTo: { kind: "sphere", id: "sphere-fire" } }

WAIT! Let me re-trace this. The merge says "if theirs equals base, return theirs." But theirs didn't change appliesTo.

Actually:
   - deepEqual(theirs["Somatic"], base["Somatic"])? YES → return theirs value
   - So merged.drawbackMeta["Somatic"] = { system: "Magic", appliesTo: { kind: "sphere", id: "sphere-fire" } }
   
Hmm, that means the merge would NOT propagate mine's appliesTo change to air!

Let me re-read the merge logic more carefully...

In mergeValue (lines 77-103):
```
if (deepEqual(mine, theirs)) return mine;
if (deepEqual(mine, base)) return theirs;  // ← This is the line
if (deepEqual(theirs, base)) return mine;  // ← This is the line
```

So:
- If mine ≠ theirs but theirs = base, then return mine (the one that changed)
- If mine ≠ theirs but mine = base, then return theirs (the one that changed)

In our case:
- mine["Somatic"] = { system: "Magic", appliesTo: { kind: "sphere", id: "sphere-air" } }
- base["Somatic"] = { system: "Magic", appliesTo: { kind: "sphere", id: "sphere-fire" } }
- theirs["Somatic"] = { system: "Magic", appliesTo: { kind: "sphere", id: "sphere-fire" } }

- deepEqual(mine, theirs)? NO (appliesTo.id differs)
- deepEqual(mine, base)? NO (appliesTo.id differs)
- deepEqual(theirs, base)? YES → return mine (because theirs=base means only mine changed)

So merged["Somatic"] should be mine's version: { system: "Magic", appliesTo: { kind: "sphere", id: "sphere-air" } }

=== RESULT ===

Merged state:
- spheres: [ { id: "sphere-fire", name: "Fire", system: "Magic" } ]
- drawbacks: [ "Somatic" ]
- drawbackMeta: { "Somatic": { system: "Magic", appliesTo: { kind: "sphere", id: "sphere-air" } } }

THE PROBLEM: appliesTo refers to "sphere-air" which no longer exists in the spheres array!

=== IMPACT ===

When the read view (grantsTargeting) runs:
```
const hits = (meta?: SphereGrantMeta) => 
  meta?.appliesTo?.kind === kind && meta.appliesTo.id === id;
return {
  drawbacks: block.drawbacks.filter((d) => hits(block.drawbackMeta?.[d])),
  ...
};
```

Calling grantsTargeting(merged, "sphere", "sphere-air") returns empty arrays (no match, since sphere-air doesn't exist).
Calling grantsTargeting(merged, "sphere", "sphere-fire") also returns empty arrays (the drawback targets air, not fire).

So the read-side is correct—it doesn't crash and doesn't show false "appliesTo" flags.

HOWEVER:
- The metadata is now ORPHANED. The appliesTo reference is stale.
- If the user later re-targets the drawback, they'll see the target selector as empty (decoded from an id that no longer exists).
- The stale metadata entry persists until it's re-edited (at which point cleanup happens on line 1409/1452).

=== BENIGNNESS ASSESSMENT ===

✓ Read view (grantsTargeting) is CORRECT: it doesn't show the dangling reference.
✓ No data loss: the appliesTo is still in the metadata, just stale.
✓ No UX crash: the editor shows a blank target selector (undefined target).
✓ Cleanup happens on edit/remove: lines 1409 and 1452 delete the meta entry when the grant is removed.
✗ Orphaned metadata: the stale entry can persist indefinitely if not re-edited.

VERDICT: This is a benign edge case. The code is safe (no crashes, no data corruption, read view is correct), 
but it can leave ghost metadata entries.
